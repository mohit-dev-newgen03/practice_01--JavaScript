Heyy... Here is our one of the most important data of JS, String 😄
here i will cover few more in depth topics and practices to related the strings.


**Task 1 : Counting a character in a string without any built in method or function**

The first task for string, is counting a specific charater repition a string 
without using a method or function only through `for of` loop and basic condition check.


**Task 2 : Cleaning up messy string through a fromat Username function**

Through a function that first trims the extra spaces arround a string,
then converting that string to lower case using a built in method, all along to 
saving character by character in a new variable through a *for* loop because in js the strings are
immutable values, including underscores where ever the spaces are between in that string.